parameters:
#    app_app.example.class: App\AppBundle\Example

services:
   app_app_directory:
        class: App\AppBundle\Services\Directory\Directory
        arguments: [@security.context, $user_id]
        scope: request
   app_app_directory_namer:
        class: App\AppBundle\Services\DirectoryNamer\DirectoryNamer
        arguments: [@security.context, $user_id]
        scope: request